{{- $deploymentTag := .Values.image.tag | default .Chart.AppVersion -}}
{{- $deploymentTime := now | date "2006-01-02 15:04:05.000000" -}}

apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ template "app.fullname" . }}
  labels: {{ include "app.labels" . | nindent 4 }}

spec:
  schedule: '0 9 * * *'
  concurrencyPolicy: Replace
  suspend: false
  jobTemplate:
    spec:
      template:
        metadata:
          creationTimestamp: null
        spec:
          volumes:
            - name: log-storage
              persistentVolumeClaim:
                claimName: {{ .Values.deployment.volumes.logPVC  }}
          containers:
            - resources:
                limits:
                  cpu: {{ .Values.deployment.resources.limits.cpu  }}
                  memory: {{ .Values.deployment.resources.limits.memory  }}
                requests:
                  cpu: {{ .Values.deployment.resources.requests.cpu  }}
                  memory: {{ .Values.deployment.resources.requests.memory  }}
              name: {{ include "app.fullname" . }}
              volumeMounts:
                - name: log-storage
                  mountPath: /logs
              env:
                - name: DEPLOYMENT_TAG
                  value: {{ $deploymentTag | quote }}
                - name: DEPLOYMENT_TIME
                  value: {{ $deploymentTime | quote }}
              envFrom:
                - secretRef:
                    name: {{ .Values.deployment.env.s3Secret  }}
              imagePullPolicy: IfNotPresent
              image: {{ .Values.image.repository  }}:{{ .Values.image.tag  }}
              securityContext:
                seccompProfile:
                  type: 'RuntimeDefault'
                runAsNonRoot: true
                allowPrivilegeEscalation: false
                capabilities:
                  drop:
                    - all
                  add:
                    - NET_BIND_SERVICE
          restartPolicy: Never